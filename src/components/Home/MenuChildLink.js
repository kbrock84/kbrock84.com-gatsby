import styled from "styled-components";
import { Link } from "gatsby";

const MenuChildLink = styled(Link)`
  margin: 0;
  padding-bottom: 1px;
  color: #fefefe;
  text-decoration: none;
  font-size: 1.2em;
  font-weight: bold;
`;

export default MenuChildLink;
